//test for the grid system

spnScene(80, 0, 2.5, 5);

spnCube('basic', white, 1, 1, 1, 0, 0, 0, false, 'Cube');

spnCube('basic', white, 1, 1, 1, 0, 2, 0, true, 'Cube');

// spnGrid(globalCube.geometry.parameters.width * 2, globalCube.geometry.parameters.width * 2, globalCube.geometry.parameters.width / 2, -globalCube.geometry.parameters.height / 2, globalCube.position.z + globalCube.geometry.parameters.depth / 2);